package com.rebelfocus.core.model

/**
 * The type of focus session.
 */
enum class SessionType {
    /** Pomodoro-style session with automatic breaks. */
    Pomodoro,
    /** Open-ended focus session without structured breaks. */
    FreeForm
}
